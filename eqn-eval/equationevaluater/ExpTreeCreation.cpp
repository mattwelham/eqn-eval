#include "Main.h"
using namespace std;

void createTree(Equation equation)
{

}
/*
Equation evalEqnPriority(Equation equation)
{
	vector<BaseNode*>::iterator iter;
	for(iter = equation.vtrNodeEquation.begin(); iter != equation.vtrNodeEquation.begin(); ++iter)
	{
		BaseNode* opPtr = *iter;
		OpNode* nodeptr  = dynamic_cast<OpNode*>(opPtr);
		OpNode opNode = *opPtr;
		if(nodeptr != 0)
		{
			switch( (*opPtr)->opChar )
		}
	}

}
*/